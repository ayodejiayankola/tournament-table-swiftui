import SwiftUI

enum AppTheme {
    static let background = Color(.systemBackground)
    static let cardBackground = Color(.systemBackground)
    static let headerBackground = Color(.tertiarySystemBackground)
    static let rowAlternate = Color(.systemGray5).opacity(0.3)
    static let separator = Color(.separator)
}
